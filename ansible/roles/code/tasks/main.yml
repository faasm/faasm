---

- name: "Install system deps"
  become: yes
  apt:
    name:
      - cmake
      - git

- name: "Check out code"
  git:
    repo: "{{ remote_url | default('https://github.com/lsds/Faasm.git') }}"
    dest: "{{ ansible_env.HOME }}/faasm-bm"
    version: "{{ repo_version | default('master') }}"
    update: yes

- name: "Set up local code directory"
  become: yes
  file:
    path: "/usr/local/code"
    state: directory

- name: "Create symbolic link to repo in local code directory"
  become: yes
  file:
    src: "{{ ansible_env.HOME }}/faasm-bm"
    dest: "/usr/local/code/faasm"
    state: link
    force: yes #unlinks other users
    owner: "{{ ansible_env.USER }}"
    group: "{{ ansible_env.USER }}"
    mode: 0755

- name: "Set up filesystem directories"
  become: yes
  file:
    path: " {{ item }}"
    state: directory
    owner: "{{ ansible_user_id }}"
    group: "{{ ansible_user_id }}"
    recurse: yes
  loop:
    - "/usr/local/faasm/object"
    - "/usr/local/faasm/runtime_root"
    - "/usr/local/faasm/shared"

