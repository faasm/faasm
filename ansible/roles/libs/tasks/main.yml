---

- name: "System deps"
  become: yes
  apt:
    name: "{{ item }}"
    state: "present"
  with_items:
    - "libz-dev"

- include_tasks: pistache.yml

- include_tasks: cpp_redis.yml

- include_tasks: catch.yml
