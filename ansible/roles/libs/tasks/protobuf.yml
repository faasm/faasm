---

- name: "Install protobuf apt packages"
  become: yes
  apt:
    name: "{{ item }}"
  with_items:
    - "libprotobuf-dev"
    - "protobuf-compiler"

- name: "Hack to get protobuf shared object in the right place"