---

- name: "Install deps with apt"
  become: yes
  apt:
    name: "{{ item }}"
    state: "present"
  with_items:
    - "libz-dev"
    - "libboost-dev"
    - "libcpprest-dev"
    - "libhiredis-dev"
    - "libcgroup-dev"
    - "cgroup-tools"
