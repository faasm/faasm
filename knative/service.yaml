apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: faasm-test
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: gcr.io/knative-samples/helloworld-go
          env:
            - name: TARGET
              value: "  Foobar baz"