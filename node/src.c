int stateSize = 10;
int* stateStart;

void setState(int* memLoc) {
    stateStart = memLoc;
}

int* execute

int* allocateMem(int memSize) {
    memoryStart = malloc(memSize);
    return memoryStart;
}

int* printState() {
    printf('')
    return &data[0];
}
